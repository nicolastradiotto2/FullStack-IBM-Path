version: '3'
services:
  web:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    depends_on:
      - db

  db:
    image: mongo
    container_name: mongo_db
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
